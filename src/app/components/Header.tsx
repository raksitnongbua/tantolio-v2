"use client";
import React from "react";

const Header = () => {
  return (
    <div className="p-2 flex justify-center items-center flex-col min-h-screen">
      <h1 className="text-4xl font-semibold">
        <span>raksit</span>
        <span className="text-primary">.is()</span>
      </h1>
    </div>
  );
};

export default Header;
